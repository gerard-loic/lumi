{

    "app" : {
       "name" : "Nom du service",
       "description" : "Description du service",
       "allowed_cors_ips" : ["*"],
       "allowed_cors_methods" : ["*"],
       "allowed_cors_headers" : ["*"],
       "url" : "http://localhost:8001",
       "admin_users" : [
            {
                "username" : "username",
                "password" : "password"
            }
       ]
    },

    "authentication" : {
        "service" : "nomserviceapi",
        "jwt_secret" : "cle-secrete-jwt",
        "jwt_algorithm": "HS256"
    },

    "services" : {
        "nomserviceapi" : {
            "handler" : "LumePackAPI",
            "url" : "http://host.local",
            "timeout" : 60
        },
        "bdd" : {
            "handler" : "PostgreSQL",
            "host" : "0.0.0.0",
            "port" : 5432,
            "database" : "bddname",
            "username" : "username",
            "password" : "password"
        }
    },

    "logger" : {
        "output" : {
            "enabled" : true
        },
        "file" : {
            "enabled" : true,
            "path": "logs"
        }
    },

    "llm" : {
        "system_prompt" : "Tu es un assistant pour un extranet client. Tu réponds uniquement en français, de façon concise et professionnelle. RÈGLE ABSOLUE : Tu ne dois jamais inventer, supposer ou extrapoler des données. Toute information factuelle doit provenir exclusivement des résultats des outils. Avant de répondre à toute question portant sur des données (commandes, statuts, offres, clients…), appelle systématiquement l'outil approprié. Si aucun outil ne permet de répondre, dis explicitement que tu n'as pas cette information — ne tente jamais de répondre par déduction ou depuis tes connaissances générales. Si un outil retourne une erreur, signale-le à l'utilisateur sans tenter de combler les données manquantes. Ne jamais inventer un identifiant (uid, référence, id). Si l'utilisateur demande une action nécessitant un identifiant qu'il n'a pas fourni, demande-le avant d'appeler l'outil. Pour toutes les informations déjà présentes dans le message (date, nom, référence), appelle directement l'outil sans demander confirmation. Si le retour comprend une url affiche là toujours dans ton retour pour proposer à l'utilisateur de cliquer dessous pour voir le détail. RÈGLE ABSOLUE : Tu ne génères jamais de code sous aucune forme (Python, JavaScript, SQL, HTML, shell, pseudo-code ou tout autre langage de programmation). Si l'utilisateur envoie du code dans sa question ou te demande d'écrire, analyser, expliquer ou commenter du code, refuse poliment en lui indiquant que tu n'es pas conçu pour ce type de demande.",
        "connector" : "LiteLLM",
        "memory_messages" : 5,
        "max_tokens_month" : 20000,
        "max_requests_month" : 300,
        "max_requests_minute" : 5,
        "litellm" : {
            "model" : "openai/openai/gpt-oss-120b",
            "embedding_model" : "openai/Qwen/Qwen3-Embedding-0.6B",
            "api_base" : "https://service.fr/v1",
            "api_key" : "cle-api"
        }
    },

    "mcp" : {
        "max_tool_iterations" : 10,
        "native_tools_enabled" : [
            "search_knowledge_base",
            "generer_fichier_excel"
        ]
    },

    "files" : {
        "temp_dir" : "temp",
        "local_storage_dir" : "data"
    },

    "rag" : {
        "collection" : "demo",
        "embedding_dim" : 1024,
        "top_k" : 5,
        "chunk_size" : 500,
        "chunk_overlap" : 50,
        "connector" : "PgVector",
        "pgvector" : {
            "table" : "rag_documents"
        }
    }


    
}